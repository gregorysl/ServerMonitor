import React from "react";

const Settings = ({ item }) => {
  return (
    <div style={{ background: "#fff", padding: 5, height: "100%" }}>
      <h1>Settings</h1>
    </div>
  );
};

export default Settings;
